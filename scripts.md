Quick reference
  - npm run dev — start dev server on port 3005
  - npm run build / npm start — production build/run
  - npm run db:seed / npm run db:studio / npm run db:reset — DB helpers
  - npm run dev:setup — one-shot bootstrap (install → migrate → seed → dev) via
  scripts/dev.sh